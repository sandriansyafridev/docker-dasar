
#   Display docker image list in local repository  
    docker images  
    docker image ls

#   Pull docker image in remote repository to local repository
    docker image pull [image-name/id:tag]
    docker pull [image-name/id:tag]

#   Remove docker image in local repository
    docker image rm [image-name/id:tag]
    