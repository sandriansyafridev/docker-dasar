
#   Display docker container

    #only display running container
    docker container ls 

    #display all container running or not
    docker container ls -a 
    
    #shorthand for "docker container ls"
    docker ps  

    #shorthand for "docker container ls -a"
    docker ps -a #